node_modules/
dist/
out/
media/preview.js
media/preview.js.map
media/vendor/
*.vsix
.vscode-test/
.DS_Store

# Local-only fixtures (personal / borrowed) — never committed
fixtures/private/

# Raw screen recordings / working media — never committed
# (the README demo GIF lives under docs/ and is committed intentionally)
*.mov
*.mp4
