# Code Blocks

TypeScript example:

```ts
const user = await userRepository.findById(userId);
console.log(user.name);
```

Unknown language falls back to plain text:

```unknownlang
some :: weird >> syntax
```

Shell:

```bash
npm run compile && code --install-extension out.vsix
```
